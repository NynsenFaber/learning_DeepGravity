# Done
FeedForward Neural Networks 

# Data Preprocessing
Population data in `dati-cpa_2011` at the section level
Commuting data in Pendolarismo per sezione di censimento
PBF data per i POIs

To build the data preprocessing and save x_i. Input features for each section containing:
vector of features, position (lat, lon). The lattest to be used to compute distances.

Problems in storing the data: